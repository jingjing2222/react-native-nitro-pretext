git_url(ENV.fetch("MATCH_GIT_URL", "https://github.com/jingjing2222/ios-certificate.git"))
storage_mode("git")
git_branch(ENV.fetch("MATCH_GIT_BRANCH", "signing"))

app_identifier([ENV.fetch("APP_IDENTIFIER", "pretext.example")])
type(ENV.fetch("MATCH_TYPE", "development"))

username(ENV.fetch("FASTLANE_USER", "rlagudwjd1212@icloud.com"))
team_id(ENV.fetch("APPLE_TEAM_ID", "2YVR9BL7B6"))
git_basic_authorization(ENV["MATCH_GIT_BASIC_AUTHORIZATION"]) if ENV["MATCH_GIT_BASIC_AUTHORIZATION"]
git_bearer_authorization(ENV["MATCH_GIT_BEARER_AUTHORIZATION"]) if ENV["MATCH_GIT_BEARER_AUTHORIZATION"]
git_private_key(ENV["MATCH_GIT_PRIVATE_KEY"]) if ENV["MATCH_GIT_PRIVATE_KEY"]
